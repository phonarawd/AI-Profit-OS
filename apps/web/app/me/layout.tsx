import type { ReactNode } from "react";

/** REL-120 — leftover 5-tab chrome 제거. /me 는 Account Hub가 presentation을 소유한다. */
export default function MeLayout({ children }: { children: ReactNode }) {
  return children;
}
