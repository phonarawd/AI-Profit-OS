import { AuthSignup } from "@aipo/ui/components/auth";
import { GuestChrome } from "../../components/GuestChrome";

/** Canon auth-signup · Stage A */
export default function AuthSignupPage() {
  return (
    <GuestChrome>
      <AuthSignup />
    </GuestChrome>
  );
}
