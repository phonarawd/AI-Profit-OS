import { OnboardingFlow } from "@aipo/ui/components/onboarding";

/** Product education 7-step. Auth split chrome is not used. */
export default function OnboardingPage() {
  return <OnboardingFlow />;
}
