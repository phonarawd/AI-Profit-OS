[package]
name = "engine-rust"
version = "0.0.0"
edition = "2021"
description = "AI Profit OS settlement / match engine (Phase0 skeleton)"
publish = false

[lib]
name = "engine_rust"
path = "src/lib.rs"

[dependencies]
