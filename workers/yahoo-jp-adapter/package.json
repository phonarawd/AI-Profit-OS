{
  "name": "@aipo/yahoo-jp-adapter",
  "private": true,
  "version": "0.0.0",
  "description": "yahoo-jp-adapter — Yahoo! JAPAN Auction official partner · Phase1+ listing leg (Engine §0.0.1c)",
  "scripts": {
    "dev": "wrangler dev --config wrangler.toml",
    "deploy": "wrangler deploy --config wrangler.toml",
    "deploy:prod": "wrangler deploy --config wrangler.toml --env production"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.120.0"
  }
}
