{
  "name": "@ai-profit-os/marketing-capi-dispatcher",
  "private": true,
  "version": "0.0.0",
  "description": "Server-side CAPI dispatcher — Meta/TikTok/Google (M1+)",
  "scripts": {
    "dev": "wrangler dev --config wrangler.toml",
    "deploy": "wrangler deploy --config wrangler.toml",
    "deploy:prod": "wrangler deploy --config wrangler.toml --env production"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.120.0"
  }
}
