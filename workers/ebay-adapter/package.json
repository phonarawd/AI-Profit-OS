{
  "name": "@aipo/ebay-adapter",
  "private": true,
  "version": "0.0.0",
  "description": "ebay-adapter — Browse API multi marketplaceId · Phase1 CF deploy (ADR-003 · Engine §0.0)",
  "scripts": {
    "dev": "wrangler dev --config wrangler.toml",
    "deploy": "wrangler deploy --config wrangler.toml",
    "deploy:prod": "wrangler deploy --config wrangler.toml --env production"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.120.0"
  }
}
