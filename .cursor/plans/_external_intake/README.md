# External Intake (COPY ONLY)

```text
EXECUTION_AUTHORITY = NO
SOURCE_WRITE = FALSE
MOVE = FALSE
DO_NOT_EXECUTE = TRUE
```

이 디렉터리의 `*.plan.md`는 Cursor Home / `_archive/ai_profit_os_stale` 원본의 **바이트 동일 복사**다.
원본을 수정·삭제·실행하지 않는다. 실행 권위는 `putduk_release_master_ff3a5134.plan.md`만 가진다.
