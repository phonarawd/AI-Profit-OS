---
description: PROJECT_ISOLATION_MIRROR — AI_PROFIT_OS ↔ clime 양방향 격리 하드룰
alwaysApply: true
---

# Project Isolation Boundary (Wave: PROJECT_ISOLATION_MIRROR)

## SSOT (ALLOW — 이 프로젝트만)

| 대상 | 허용 |
|------|------|
| Workspace root | `C:\Users\PC\Desktop\AI_PROFIT_OS` (실제 루트 절대경로만) |
| GitHub | `phonarawd/AI-Profit-OS` only |
| Supabase | `mgsytcetsiecllmhcyox` only |
| Cursor project cache | `~/.cursor/projects/<이프로젝트>` only |
| Plan SSOT | 레포 `.cursor/plans/` only |

## DENY (읽·수정·비교·목록·Shell/Grep/MCP 금지)

- `C:\Users\PC\Desktop\clime-gb`
- `phonarawd/clime-gb`
- Supabase `project_ref=qrvanbyjgflaugdaslqh`
- 전역 `%USERPROFILE%\.cursor\plans\**` 전체
- 경로 마커: `clime-gb`, `clime_`, `/clime/`, `qrvanbyjgflaugdaslqh`
- 레포 안 `clime_*.plan.md` / `*clime-gb*` plan
- Desktop multi-root에 클라이 폴더 추가

## 하드룰

1. 클라이 디스크·GitHub·Supabase·전역 plans를 열어 “확인”하지 않는다.
2. DENY 검증은 hook fixture(경로 문자열)만 — 실경로 Read/Shell 금지.
3. failClosed 훅: 빈 stdin/비JSON → allow · 항상 exit 0 · 차단은 `permission:deny`만 · 정책=순수함수.
4. account-wide GitHub/Supabase CLI라도 외국(클라이) 타깃은 DENY.
5. 클라이 소스/플랜을 읽어 베끼지 않는다.

## 훅 SSOT

- 정책: `.cursor/hooks/lib/project-boundary-policy.mjs`
- 엔트리: `.cursor/hooks/project-boundary.mjs`
- 배선: `.cursor/hooks.json` (`failClosed: true`)
- 검증: `pnpm verify:project-boundary` (fixture only)
