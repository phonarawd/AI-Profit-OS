name: spark-global-ui-qa

permissions:
  contents: read

on:
  pull_request:
    branches: [main]

jobs:
  admin-spark-qa:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Backend-only skip
        run: echo "UI moved to phonarawd/putduk-web. Required job name kept. No Next/Playwright build."
